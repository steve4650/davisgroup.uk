This is a repo for a webpage (https://davisgroup.uk) with a ton of sub-projects.

`uv run make.py build` builds the whole project. Test this works.

`uv run make.py lint` lints and checks the repo's formatting and `uv run make.py fmt` fixes everything mostly.
